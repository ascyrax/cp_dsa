#include <bits/stdc++.h>
using namespace std;

typedef long long ll;
typedef long double ld;

#define size(x) (int)(x).size()
#define endl "\n"
#define ioss                         \
        ios::sync_with_stdio(false); \
        cin.tie(0);
#define pb push_back

double startTime;
double gct() //get_current_time
{
        return ((double)clock() - startTime) / CLOCKS_PER_SEC;
}
void suraj();

int main()
{
        startTime = (double)clock();
        //freopen("shell.in","r",stdin);freopen("shell.out","w",stdout);
        ioss
        //int t;cin>>t;for(int i=1;i<=t;i++)
        {
                //cout<<"Case #"<<i<<": ";
                suraj();
        }
        return 0;
}

// @author: ascyrax

const ll mod = 1e9 + 7;

//...............................

void suraj()
{
        float a = 119999999;
        float b = 333333312.0000000000000000000000000000000000;
        // float c = a + 1;
        double d = a + 1;
        double e = b + 1;
        // cout << c << endl;
        cout << d << endl;
        // cout << fixed << setprecision(20) << c << endl;
        cout << fixed << setprecision(20) << d << endl;
        cout << fixed << setprecision(20) << a + 1 << endl;
        cout << fixed << setprecision(20) << b + 1 << endl;
        cout << fixed << e << endl;
        cout << setprecision(20) << e << endl;
        cout << fixed << setprecision(20) << e << endl;
}

//................................
