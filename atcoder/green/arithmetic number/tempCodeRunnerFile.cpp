ut << "arithNums.size() = " << arithNums.size() << endl;
        // // sort(arithNums.begin(), arithNums.end());
        // for (auto el : arithNums)
        //         cout << el << " ";
        // cout << endl;